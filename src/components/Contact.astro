---

---

<div class="region" style="--spot-color: var(--gray200);" id="contact">
  <div class="wrapper flow prose" style="--flow-space: var(--space-l);">
    <div class="switcher">
      <div class="flow prose">
        <h2>Reach out</h2>
        <p>
          Looking to get in touch for consulting? Have a question or comment
          about a topic I've discussed here? Feel free to drop me a line!
        </p>
      </div>
      <div class="flow prose">
        <form
          name="main"
          netlify-honeypot="bot-field"
          class="switcher"
          method="POST"
          data-netlify="true"
          action="/"
        >
          <input type="hidden" name="subject" value="Form: frankstall.one" />
          <p class="hidden">
            <label for="bot-field">Do you like delicious treats?</label>
            <input name="bot-field" id="bot-field" />
          </p>
          <span class="stack">
            <label for="name">Name</label>
            <input id="name" type="text" required />
          </span>
          <span class="stack">
            <label for="email">Email</label>
            <input id="email" type="email" required />
          </span>
          <span class="stack">
            <label for="tel">Phone number</label>
            <input id="tel" type="tel" />
          </span>
          <span class="stack">
            <label for="message">Message</label>
            <textarea name="message" id="message" rows="6" required></textarea>
          </span>
          <button
            type="submit"
            class="submit-button"
            style="--spot-color: var(--color-loud);">Submit</button
          >
        </form>
      </div>
    </div>
  </div>
</div>
