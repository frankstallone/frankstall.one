export type Header = {
  meta: {
    title?: string;
    description?: string;
    canonicalURL?: string;
    image?: string;
    robots?: string;
  };
};

export type SubPages = {
  subtitle?: string;
};
